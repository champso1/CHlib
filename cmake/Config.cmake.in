# boiler plate, filled it automatically
@PACKAGE_INIT@

# include the exported targets, all the important stuff
include("${CMAKE_CURRENT_LIST_DIR}/CHlibTargets.cmake")

# set custom variables to be used in other projects' cmake files
set(CHlib_INCLUDE_DIRS "${CMAKE_CURRENT_LIST_DIR}/../../../include")
set(CHlib_LIBRARIES CHlib::CHlib)
